pipeline {

    agent any
    
    stages {

        stage('Git Branch') {

            steps {

                echo "$GIT_BRANCH"
                sh(scrtipt: 'echo Hello World')
            }
        }
    }
}
